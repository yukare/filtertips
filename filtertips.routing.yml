# Route for settings page.
filtertips.settings:
  path: '/admin/config/filtertips/settings'
  defaults:
    _form: '\Drupal\filtertips\Form\Settings'
    _title: 'Settings'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE

